/* Oscar Accounting Cloud Sync v1 - tenant isolated, local-first, delta live sync */
(()=>{'use strict';
const VERSION='OscarSyncV1';
const STORES=new Set(['products','categories','warehouses','stock','stock_movements','invoices','purchases','customers','suppliers','partner_statements','accounts','transfers','expenses','shifts','audit_logs','held_invoices','settings','vouchers','employees']);
const META_PREFIX='oscar_sync_meta_v1::', PENDING_PREFIX='oscar_sync_pending_v1::', DEVICE_KEY='oscar_sync_device_v1';
let bridge=null, initialized=false, busy=false, suppress=false, syncTimer=null, probeTimer=null, bc=null, schemaTenant='', seq=0, lastProbe=0;
const safe=v=>String(v??'').trim();
const clone=v=>v==null?v:JSON.parse(JSON.stringify(v));
const tenant=()=>safe(window.OscarActivation?.readRuntime?.()?.companyId||'');
const cfg=()=>window.OscarActivation?.readDatabaseAccess?.(tenant())||window.OscarActivation?.readRuntime?.()?.database||null;
function deviceId(){let x=localStorage.getItem(DEVICE_KEY);if(!x){x='DEV-'+(crypto.randomUUID?crypto.randomUUID():Date.now()+'-'+Math.random().toString(36).slice(2));localStorage.setItem(DEVICE_KEY,x)}return x}
function metaKey(t=tenant()){return META_PREFIX+encodeURIComponent(t||'none')}
function pendingKey(t=tenant()){return PENDING_PREFIX+encodeURIComponent(t||'none')}
function readMeta(){try{return JSON.parse(localStorage.getItem(metaKey())||'{}')}catch(_){return{}}}
function writeMeta(m){localStorage.setItem(metaKey(),JSON.stringify(m||{}))}
function readPending(){try{return JSON.parse(localStorage.getItem(pendingKey())||'{}')}catch(_){return{}}}
function writePending(p){localStorage.setItem(pendingKey(),JSON.stringify(p||{}));emitStatus()}
function pendingCount(){return Object.keys(readPending()).length}
function pendingItems(){return Object.entries(readPending()).sort((a,b)=>Number(b[1]?.rev||0)-Number(a[1]?.rev||0)).map(([id,o])=>({id,entity:o.store,syncId:String(o.rev||id),timestamp:o.timestamp||Date.now(),action:o.deleted?'delete':'upsert',data:o.value||null,status:'pending'}))}
function nextRev(){const now=Date.now()*1000; seq=(seq+1)%900; return now+seq}
function keyString(store, value, explicit){if(explicit!==undefined&&explicit!==null){if(store==='stock')return JSON.stringify(Array.isArray(explicit)?explicit:[explicit]);return String(explicit)}if(store==='stock')return JSON.stringify([value?.productId||'',value?.warehouseId||'']);if(store==='settings')return String(value?.key||'store_config');return String(value?.id||'')}
function actualKey(store,k){if(store==='stock'){try{return JSON.parse(k)}catch(_){return String(k)}}return k}
const pk=(store,key)=>store+'\u0001'+key;
const prefix=()=>`oscar/companies/${encodeURIComponent(tenant())}/d/`;
const pathFor=(store,key)=>prefix()+encodeURIComponent(store)+'/'+encodeURIComponent(key);
function parsePath(path){const p=String(path||''),pre=prefix();if(!p.startsWith(pre))return null;const rest=p.slice(pre.length),cut=rest.indexOf('/');if(cut<1)return null;try{return{store:decodeURIComponent(rest.slice(0,cut)),key:decodeURIComponent(rest.slice(cut+1))}}catch(_){return null}}
function emitStatus(extra={}){const detail={version:VERSION,pending:pendingCount(),busy,isOnline:navigator.onLine!==false,tenant:tenant(),...extra};try{window.dispatchEvent(new CustomEvent('oscar:sync-status',{detail}))}catch(_){} }
function broadcast(type){try{bc?.postMessage?.({type,tenant:tenant(),deviceId:deviceId(),at:Date.now()})}catch(_){} }
function captureStoreChange(store,value,{deleted=false,key}={}){if(suppress||!STORES.has(store)||!tenant())return false;const k=keyString(store,value,key);if(!k)return false;const rev=nextRev(),p=readPending();p[pk(store,k)]={store,key:k,deleted:!!deleted,value:deleted?null:clone(value),rev,deviceId:deviceId(),timestamp:Date.now(),attempts:0};writePending(p);broadcast('local-change');requestSync(100);return true}
async function ensureSchema(){const t=tenant(), d=window.OscarActivation?.tursoDirect, c=cfg();if(!t||!d||!c?.databaseURL||!c?.authToken)throw Error('بيانات مزامنة الشركة غير متاحة.');const table=d.table(c),metaTable=table+'_syncmeta';if(schemaTenant===t)return{d,c,table,metaTable};await d.ensure(c);const [info]=await d.pipeline(c,[{sql:`PRAGMA table_info(${table})`,args:[]}]);const cols=d.rows(info).map(x=>String(x.name));if(!cols.includes('sync_batch')){try{await d.pipeline(c,[{sql:`ALTER TABLE ${table} ADD COLUMN sync_batch INTEGER NOT NULL DEFAULT 0`,args:[]}])}catch(e){if(!/duplicate column|already exists/i.test(String(e?.message||e)))throw e}}await d.pipeline(c,[{sql:`CREATE TABLE IF NOT EXISTS ${metaTable} (id INTEGER PRIMARY KEY CHECK(id=1),batch INTEGER NOT NULL DEFAULT 0)`,args:[]},{sql:`INSERT OR IGNORE INTO ${metaTable}(id,batch) VALUES(1,0)`,args:[]},{sql:`CREATE INDEX IF NOT EXISTS idx_${table}_sync_batch ON ${table}(sync_batch,path)`,args:[]}]);schemaTenant=t;return{d,c,table,metaTable}}
async function remoteBatch(s){const[r]=await s.d.pipeline(s.c,[{sql:`SELECT batch FROM ${s.metaTable} WHERE id=1`,args:[]}]);return Number(s.d.rows(r)[0]?.batch||0)}
function buildStatements(s,batch){const st=[{sql:'BEGIN IMMEDIATE',args:[]},{sql:`UPDATE ${s.metaTable} SET batch=batch+1 WHERE id=1`,args:[]}];for(const [id,o] of batch){const env={v:o.deleted?null:o.value,deleted:!!o.deleted,rev:Number(o.rev||nextRev()),deviceId:o.deviceId||deviceId()};st.push({sql:`INSERT INTO ${s.table}(path,payload,deleted,updated_at,sync_batch) VALUES(?,?,?,?,(SELECT batch FROM ${s.metaTable} WHERE id=1)) ON CONFLICT(path) DO UPDATE SET payload=excluded.payload,deleted=excluded.deleted,updated_at=excluded.updated_at,sync_batch=excluded.sync_batch WHERE excluded.updated_at>=${s.table}.updated_at`,args:[pathFor(o.store,o.key),JSON.stringify(env),o.deleted?1:0,Number(o.rev||nextRev())]})}st.push({sql:'COMMIT',args:[]});return st}
async function pushPending(){const all=Object.entries(readPending());if(!all.length)return{uploaded:0,remaining:0};const s=await ensureSchema();let uploaded=0;for(let i=0;i<all.length;i+=60){const batch=all.slice(i,i+60);try{await s.d.pipeline(s.c,buildStatements(s,batch),Math.max(30000,batch.length*700));const latest=readPending();for(const[id,o]of batch){if(Number(latest[id]?.rev||0)===Number(o.rev||0)){delete latest[id];uploaded++}}writePending(latest)}catch(err){console.warn('[OscarSync] batch failed',err);for(const pair of batch){try{await s.d.pipeline(s.c,buildStatements(s,[pair]),30000);const latest=readPending(),[id,o]=pair;if(Number(latest[id]?.rev||0)===Number(o.rev||0)){delete latest[id];uploaded++}writePending(latest)}catch(e){const latest=readPending(),cur=latest[pair[0]];if(cur){cur.attempts=Number(cur.attempts||0)+1;cur.lastError=String(e?.message||e).slice(0,300);latest[pair[0]]=cur;writePending(latest)}}}}}return{uploaded,remaining:pendingCount()}}
async function applyRows(rows,batch){if(!bridge)return{applied:0};const pending=readPending();let applied=0;const touched=new Set();suppress=true;try{for(const row of rows){const parsed=parsePath(row.path);if(!parsed||!STORES.has(parsed.store))continue;let env=null;try{env=typeof row.payload==='string'?JSON.parse(row.payload):row.payload}catch(_){env=null}const remoteRev=Number(row.updated_at||env?.rev||0),id=pk(parsed.store,parsed.key),local=pending[id];if(local&&Number(local.rev||0)>remoteRev)continue;const deleted=Number(row.deleted)===1||env?.deleted===true;if(deleted)await bridge.deleteFromStore(parsed.store,actualKey(parsed.store,parsed.key),false);else if(env&&Object.prototype.hasOwnProperty.call(env,'v'))await bridge.putInStore(parsed.store,env.v,false);else continue;applied++;touched.add(parsed.store)}}finally{suppress=false}const m=readMeta();m.remoteBatch=Math.max(Number(m.remoteBatch||0),Number(batch||0));m.batchInitialized=true;m.lastPullAt=Date.now();writeMeta(m);if(applied){try{await bridge.onApplied?.([...touched])}catch(_){}try{window.dispatchEvent(new CustomEvent('oscar:sync-applied',{detail:{applied,stores:[...touched],remoteBatch:batch}}))}catch(_){}broadcast('synced')}return{applied,changedStores:[...touched]}}
async function pullChanges({force=false}={}){const s=await ensureSchema(),m=readMeta(),remote=await remoteBatch(s),last=Number(m.remoteBatch||0),pre=prefix(),hi=pre+'\uffff';if(!force&&m.batchInitialized&&remote<=last)return{applied:0,remoteBatch:remote,remoteRows:0};let r;if(!m.batchInitialized||force&&last===0){[r]=await s.d.pipeline(s.c,[{sql:`SELECT path,payload,deleted,updated_at,sync_batch FROM ${s.table} WHERE path>=? AND path<? ORDER BY path`,args:[pre,hi]}],60000)}else{[r]=await s.d.pipeline(s.c,[{sql:`SELECT path,payload,deleted,updated_at,sync_batch FROM ${s.table} WHERE path>=? AND path<? AND sync_batch>? ORDER BY sync_batch,path`,args:[pre,hi,last]}],60000)}const rows=s.d.rows(r);const out=await applyRows(rows,remote);if(!rows.length){m.remoteBatch=remote;m.batchInitialized=true;m.lastPullAt=Date.now();writeMeta(m)}return{...out,remoteBatch:remote,remoteRows:rows.length}}
async function syncNow({manual=false,force=false}={}){if(busy)return{busy:true,remaining:pendingCount()};if(!tenant())return{unavailable:true};if(navigator.onLine===false){emitStatus({state:'offline'});return{offline:true,remaining:pendingCount()}}busy=true;emitStatus({state:'syncing'});try{const pushed=pendingCount()?await pushPending():{uploaded:0,remaining:0};const pulled=await pullChanges({force:!!force});const result={...pushed,...pulled,remaining:pendingCount(),success:true};emitStatus({state:'success',lastSuccessAt:Date.now(),result});return result}catch(e){console.error('[OscarSync]',e);emitStatus({state:'error',message:String(e?.message||e)});return{error:true,message:String(e?.message||e),remaining:pendingCount()}}finally{busy=false;emitStatus()}}
function requestSync(delay=120){if(!tenant()||navigator.onLine===false)return;clearTimeout(syncTimer);syncTimer=setTimeout(()=>syncNow({force:true}).catch(()=>{}),Math.max(60,delay))}
async function checkRemote({force=false}={}){if(busy||!tenant()||navigator.onLine===false||document.visibilityState==='hidden')return;const now=Date.now();if(!force&&now-lastProbe<850)return;lastProbe=now;if(pendingCount())return syncNow({force:true});try{const s=await ensureSchema(),r=await remoteBatch(s),m=readMeta();if(!m.batchInitialized||r>Number(m.remoteBatch||0))return syncNow({force:false})}catch(e){emitStatus({state:'error',message:String(e?.message||e)})}}
function startProbe(){if(probeTimer)return;const tick=async()=>{try{await checkRemote()}catch(_){}probeTimer=setTimeout(tick,1000)};probeTimer=setTimeout(tick,650)}
async function initialize(opts={}){bridge=opts.bridge||bridge;if(!tenant()||!bridge)return{tenant:tenant(),unavailable:true};initialized=true;setupBroadcast();startProbe();emitStatus({state:'ready'});if(navigator.onLine===false)return{tenant:tenant(),offline:true,remoteRows:0};const pulled=await pullChanges({force:!readMeta().batchInitialized});if(pendingCount())requestSync(80);return{tenant:tenant(),...pulled,remaining:pendingCount()}}
function setupBroadcast(){try{bc?.close?.();bc='BroadcastChannel'in window?new BroadcastChannel('oscar-cloud-sync-v1'):null;if(bc)bc.onmessage=e=>{const m=e.data||{};if(m.tenant!==tenant()||m.deviceId===deviceId())return;if(m.type==='local-change'||m.type==='synced')setTimeout(()=>checkRemote({force:true}),m.type==='synced'?20:150)}}catch(_){bc=null}}
function resetForTenant(){schemaTenant='';lastProbe=0;setupBroadcast();emitStatus({state:'tenant-reset'})}
window.addEventListener('online',()=>{emitStatus({state:'online'});pendingCount()?requestSync(60):checkRemote({force:true})});
window.addEventListener('offline',()=>emitStatus({state:'offline'}));
document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')setTimeout(()=>checkRemote({force:true}),80)});
window.addEventListener('focus',()=>checkRemote({force:true}));
window.OscarCloudSync={version:VERSION,initialize,syncNow,checkRemote,requestSync,pendingCount,pendingItems,captureStoreChange,resetForTenant,get busy(){return busy},get suppress(){return suppress}};
})();